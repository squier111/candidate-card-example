import styled from 'styled-components';

export const ProfileSource = styled('div')`
	color: ${(props) => props.theme.colors.functional.text.subdued};
	font-size: 14px;
	line-height: 16px;
	margin-bottom: 0;
`;
